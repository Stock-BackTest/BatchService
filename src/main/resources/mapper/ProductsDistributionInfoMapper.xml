<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
  namespace="com.stockbacktest.batchservice.job.transformetfdailyprice.domain.mapper.ProductsDistributionInfoMapper">
  <insert id="insertList">
    INSERT INTO PRODUCTS_DISTRIBUTION_INFO (
    ISIN_CD,
    RECORD_DATE,
    PAYABLE_DATE,
    CASH_PER_SHARE,
    PAYOUT_YIELD_PCT,
    TAX_BASIS,
    DISTRIBUTION_TYPE,
    CREATED_AT,
    )
    VALUES
    <foreach collection="list"
      item="item"
      separator=",">
      (
      #{item.isinCd},
      #{item.recordDate},
      #{item.payableDate},
      #{item.cashPerShare},
      #{item.payoutYieldPct},
      #{item.taxBasis},
      #{item.distributionType},
      #{item.createdAt},
      )
    </foreach>
  </insert>
</mapper>