spring:
  profiles:
    active: test

  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:

  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: false

jasypt:
  encryptor:
    password: "test-secret-password"

krx:
  apiUrl: initial-test-krx-url
  apikey: initial-test-api-key